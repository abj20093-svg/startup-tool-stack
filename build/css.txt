
:root{
  --violet:#5B4CDB; --violet-dim:#3A2D8A; --violet-deep:#2D246C; --violet-light:#EEEBFB;
  --ink:#17143A; --ink-2:#44415E; --slate:#6E6B85; --slate-2:#9B98B0;
  --white:#FFF; --off:#F7F6FC; --surface:#F2F1F9; --border:#E7E5F1; --border-d:#D6D3E6;
  --rose:#B42B54; --rose-light:#FBF1F5;
  --col:620px;
  --sans:'Inter',-apple-system,"SF Pro Text","Helvetica Neue","Segoe UI",system-ui,sans-serif;
}
*{margin:0;padding:0;box-sizing:border-box}
html{scroll-behavior:smooth}
@media (prefers-reduced-motion: reduce){html{scroll-behavior:auto}*{transition:none !important}}
body{font-family:var(--sans);color:var(--ink);background:var(--white);font-size:15px;line-height:1.6}
.wrap{max-width:1060px;margin:0 auto;padding:0 24px}
a{color:var(--violet)}
a:hover{color:var(--violet-dim)}
:focus-visible{outline:2px solid var(--violet);outline-offset:2px;border-radius:2px}
.skip{font-weight:600;position:absolute;left:-9999px;top:0;background:var(--violet);color:#fff;padding:8px 14px;z-index:99;font-family:var(--sans)}
.skip:focus{left:8px;top:8px}
.eyebrow{font-family:var(--sans);font-weight:600;font-size:12px;letter-spacing:.14em;text-transform:uppercase;color:var(--violet)}
.muted{color:var(--ink)}
.num{font-variant-numeric:tabular-nums}

/* ---------- masthead ---------- */
.mast{border-bottom:1px solid var(--border);padding:56px 0 40px}
.mast h1{font-family:var(--sans);font-weight:700;font-size:34px;letter-spacing:-.022em;line-height:1.08;margin-top:6px}
.scopeline a{color:var(--violet)}
.byline{font-weight:400;margin-top:8px;font-family:var(--sans);font-size:13px;color:var(--slate)}

.searchwrap{position:relative;display:block;flex:0 1 400px;min-width:300px}
.results.on{display:block}
.results li+li{border-top:1px solid var(--border)}
.results button:hover,.results button.active{background:var(--violet-light)}
.backfinder.on{display:block}

/* ---------- front matter ---------- */
.howto{padding:40px 0;border-bottom:1px solid var(--border)}
.howto h2{font-family:var(--sans);font-weight:700;font-size:22px;letter-spacing:-.012em;margin-bottom:14px}
.howto p{margin-bottom:10px;max-width:72ch;color:var(--ink-2)}
.contents{padding:40px 0;border-bottom:1px solid var(--border)}
.contents h2{font-family:var(--sans);font-weight:700;font-size:22px;letter-spacing:-.012em;margin-bottom:6px}
.contents .grp{margin-top:14px}
.contents table{width:100%;border-collapse:collapse;max-width:680px}
.contents td{font-weight:600;padding:7px 6px;border-bottom:1px solid var(--border);font-family:var(--sans);font-size:15px;vertical-align:baseline}
.contents td.cnum{width:44px;color:var(--violet);font-weight:600}
.contents td.soon{color:var(--slate)}
.contents tr:hover td{background:var(--off)}
.contents td a{text-decoration:none;color:var(--ink);display:block}
.contents td a:hover{color:var(--violet)}

/* ---------- chapter ---------- */
.chapter{padding-top:8px}
.chap-open{padding:46px 0 28px;border-bottom:1px solid var(--border)}
.chap-open h2{font-family:var(--sans);font-weight:700;font-size:30px;letter-spacing:-.02em;margin-top:6px}
.chap-open h2::after{content:"";display:block;width:38px;height:3px;border-radius:2px;background:var(--violet);margin-top:9px;transition:width .25s ease}
.chap-open:hover h2::after{width:76px}
.chap-open .lineup{font-weight:600;margin-top:14px;font-family:var(--sans);font-size:14px;color:var(--ink)}
.chap-open .lineup b{color:var(--ink);font-weight:600}

/* ---------- at a glance: contained table ---------- */
.glance{padding:38px 0;border-bottom:1px solid var(--border)}
.glance h3{font-family:var(--sans);font-weight:700;font-size:20px;letter-spacing:-.012em;margin-bottom:14px}
.glance .gcap{font-weight:400;font-family:var(--sans);font-size:13px;color:var(--slate);margin-top:12px}
table.gtable{width:100%;border-collapse:separate;border-spacing:0;border:1px solid var(--border);border-radius:12px}
.gtable th{font-family:var(--sans);font-weight:600;font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--slate);text-align:left;padding:10px 14px;background:var(--off);border-bottom:1px solid var(--border)}
.gtable th:first-child{border-top-left-radius:12px}
.gtable th:last-child{border-top-right-radius:12px}
.gtable td{padding:12px 14px;border-bottom:1px solid var(--border);vertical-align:top}
.gtable tr:last-child td{border-bottom:none}
.gtable tbody tr:hover td{background:var(--off)}
.gtable tr:last-child td:first-child{border-bottom-left-radius:12px}
.gtable tr:last-child td:last-child{border-bottom-right-radius:12px}
.gtable td.tname{font-family:var(--sans);font-weight:700;font-size:15px}
.gtable td.tname a{color:var(--ink);text-decoration:none}
.gtable td.tname a:hover{color:var(--violet)}
.glead{font-family:var(--sans);font-weight:600;font-size:14px;line-height:1.35;color:var(--ink);display:block}
.gdetail{display:block;font-size:13px;line-height:1.42;color:var(--ink-2);margin-top:3px}
.gtable .num .glead{font-variant-numeric:tabular-nums}

/* ---------- profile ---------- */
.profile{padding:42px 0;border-bottom:1px solid var(--border)}
.phead{margin-top:8px;margin-bottom:16px}
.phead h4{font-family:var(--sans);font-weight:700;font-size:24px;letter-spacing:-.016em}
.phead h4::after{content:"";display:block;width:30px;height:3px;border-radius:2px;background:var(--violet);margin-top:7px;transition:width .25s ease}
.phead:hover h4::after{width:60px}
.psec{margin-top:22px;max-width:none}
.pseclabel{font-family:var(--sans);font-weight:600;font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--violet-dim);margin:2px 0 10px}
.pseclabel::after{content:"";display:block;width:26px;height:2px;border-radius:1px;background:var(--violet);margin-top:5px}
.psec p{max-width:70ch;margin-bottom:8px}
b,strong{font-weight:600}
.pl{font-family:var(--sans);font-weight:600;font-size:15px}
.na{color:var(--ink)}

/* decision callout */
.ledger{margin-top:24px;border:1px solid var(--border);border-left:3px solid var(--violet);border-radius:12px;padding:18px 20px;background:var(--white)}
.ledger .lhead{font-family:var(--sans);font-weight:600;font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--violet)}
.ledger .ideal{margin-top:8px;font-size:15px;max-width:70ch}
.ledger .pair{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}
.pcell{border-radius:10px;padding:13px 15px}
.pcell.go{background:var(--violet-light)}
.pcell.stop{background:var(--rose-light)}
.pcell.go .plabel{color:var(--violet-dim)}
.pcell.stop .plabel{color:var(--rose)}
.ledger .plabel{font-family:var(--sans);font-weight:600;font-size:11px;letter-spacing:.08em;text-transform:uppercase;margin-bottom:6px}
.ledger p{margin-bottom:8px}
.pcell .fb>li::before{background:var(--violet-dim)}
.pcell.stop .fb>li::before{background:var(--rose)}
.pcell .fb>li:last-child{margin-bottom:0}

/* factor bands */
.bands{margin-top:24px}
.bands .tcol{display:contents}
.bands.b2{margin-top:0}
.factor{break-inside:avoid;margin-bottom:20px}
ul.fb{list-style:none;margin:0;padding:0}
.fb>li{position:relative;padding-left:16px;margin:0 0 9px;font-size:15px}
.fb>li::before{content:"";position:absolute;left:1px;top:.52em;width:5px;height:5px;border-radius:50%;background:var(--violet)}
.fb>li>ul{list-style:none;margin:5px 0 2px;padding:0}
.fb>li>ul>li{position:relative;padding-left:14px;margin:0 0 4px;font-size:14px;line-height:1.55;color:var(--ink-2)}
.fb>li>ul>li::before{content:"";position:absolute;left:0;top:.68em;width:6px;height:2px;border-radius:1px;background:var(--border-d)}
.factor p{max-width:70ch;font-size:15px;line-height:1.55;margin-bottom:6px}
.band{margin-bottom:18px}
.bandlabel{break-after:avoid;font-family:var(--sans);font-weight:600;font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--violet-dim);margin:2px 0 10px}
.bandlabel::after{content:"";display:block;width:26px;height:2px;border-radius:1px;background:var(--violet);margin-top:5px}
.runhead{display:none}
.pfoot{font-weight:400;display:none}

/* filtered-out state */
.hidden-by-filter{display:none}
.dim-by-filter{opacity:.35}

/* ---------- mobile ---------- */
@media (max-width:700px){
  .gtable thead{display:none}
  .gtable{border:none;border-radius:0}
  .gtable,.gtable tbody,.gtable tr,.gtable td{display:block;width:100%}
  .gtable tr{border:1px solid var(--border);border-radius:12px;padding:14px 16px;margin-bottom:14px}
  .gtable td{border:none;padding:8px 0}
  .gtable tbody tr:hover td{background:transparent}
  .gtable td.tname{width:auto;font-size:17px;padding-top:0}
  .gtable td[data-label]::before{content:attr(data-label);display:block;font-family:var(--sans);font-weight:600;font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--slate);margin-bottom:3px}
  .bands{display:block}
  .ledger .pair{grid-template-columns:1fr}
  .mast h1{font-size:28px}
}

/* ---------- print base resets ---------- */

.mast{border-bottom:none}
.contents.mastcontents{border-bottom:none;padding-bottom:0}

/* ---------- screen navigation (hidden in print) ---------- */
.topbar{position:sticky;top:0;z-index:80;display:flex;align-items:center;justify-content:space-between;
  gap:16px;padding:10px 20px;background:rgba(255,255,255,.94);backdrop-filter:saturate(180%) blur(8px);
  border-bottom:1px solid var(--border)}
.tb-home{font-family:var(--sans);font-weight:700;font-size:14px;color:var(--ink);text-decoration:none}
.jump{font-weight:600;font-family:var(--sans);font-size:14px;position:relative}
.jump>summary{cursor:pointer;list-style:none;font-weight:600;color:var(--violet);
  border:1px solid var(--border-d);border-radius:8px;padding:7px 14px;min-height:38px;display:inline-flex;
  align-items:center;gap:8px;background:var(--white)}
.jump>summary::-webkit-details-marker{display:none}
.jump>summary::after{content:"▾";font-size:11px}
.jump[open]>summary::after{content:"▴"}
.jump ul{list-style:none;margin:6px 0 0;padding:6px;background:var(--white);border:1px solid var(--border-d);
  border-radius:10px;box-shadow:0 6px 24px rgba(23,20,58,.10)}
.navmenu ul{position:absolute;right:0;top:calc(100% + 6px);min-width:290px;max-height:70vh;overflow:auto;z-index:90}
.jump li a{display:block;padding:8px 12px;border-radius:6px;color:var(--ink);text-decoration:none}
.jump li a:hover{background:var(--violet-light);color:var(--violet-dim)}

a.site{color:inherit;text-decoration:none;border-bottom:2px solid var(--violet-light)}
a.site:hover{color:var(--violet);border-bottom-color:var(--violet)}
a.site::after{content:"↗";font-size:.55em;vertical-align:super;margin-left:3px;color:var(--violet)}

/* ---------- two-level chapter menu ---------- */
.navmenu ul{position:absolute;right:0;top:calc(100% + 6px);width:340px;max-height:76vh;overflow:auto;z-index:90;padding:8px}
.navgrp>details>summary{cursor:pointer;list-style:none;font-family:var(--sans);font-weight:600;font-size:14px;
  padding:9px 12px;border-radius:7px;color:var(--ink);display:flex;justify-content:space-between;align-items:center}
.navgrp>details>summary::-webkit-details-marker{display:none}
.navgrp>details>summary::after{content:"+";color:var(--slate);font-weight:700}
.navgrp>details[open]>summary{color:var(--violet);background:var(--violet-light)}
.navgrp>details[open]>summary::after{content:"–"}
.navtools{display:flex;flex-direction:column;padding:2px 0 8px 10px;margin-left:8px;border-left:2px solid var(--violet-light)}
.navtools a{font-weight:400;font-family:var(--sans);font-size:13px;padding:7px 12px;border-radius:6px;color:var(--ink-2);text-decoration:none}
.navtools a:hover{background:var(--violet-light);color:var(--violet-dim)}
.navall{font-weight:400;color:var(--violet) !important}
.navflat{display:block;font-family:var(--sans);font-weight:600;font-size:14px;padding:9px 12px;
  border-radius:7px;color:var(--ink);text-decoration:none}
.navflat:hover{background:var(--violet-light);color:var(--violet)}

/* ---------- anchor offset so targets clear the sticky bar ---------- */
[id]{scroll-margin-top:76px}
/* ---------- persistent side rail ---------- */
.siderail{display:none}
.rail-home{display:block;font-family:var(--sans);font-weight:700;font-size:15px;color:var(--ink);text-decoration:none}
.railnav{list-style:none;margin:0;padding:0}
.railnav .navgrp>details>summary{padding:8px 10px}
.railnav .navtools a{font-size:13px;padding:6px 10px}
.railnav .navflat{padding:8px 10px}
@media screen and (min-width:1000px){
  .siderail{display:block;position:fixed;top:0;left:0;bottom:0;width:264px;overflow-y:auto;z-index:70;
    padding:20px 16px 40px;background:var(--off);border-right:1px solid var(--border)}
  body{padding-left:264px}
  .topbar{display:none}
}

/* ---------- rail: sections, active states ---------- */
.navsec{font-family:var(--sans);font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;
  color:var(--slate-2);padding:16px 10px 6px;list-style:none}
.railnav .navgrp>details>summary{display:flex;gap:9px;align-items:baseline}
.navgrp .cn{font-family:var(--sans);font-size:11px;font-weight:700;color:var(--slate-2);min-width:17px}
.navgrp .ct{flex:1}
.navgrp.is-active>details>summary{background:var(--violet);color:#fff;border-radius:7px}
.navgrp.is-active>details>summary .cn{color:rgba(255,255,255,.72)}
.navgrp.is-active>details>summary::after{color:rgba(255,255,255,.8)}
.navtools a.is-here{background:var(--violet-light);color:var(--violet-dim);font-weight:600;
  box-shadow:inset 2px 0 0 var(--violet)}
.railnav .navgrp>details>summary:focus-visible,a.site:focus-visible{
  outline:2px solid var(--violet);outline-offset:2px}
/* ---------- reading progress ---------- */
.progress{position:fixed;top:0;left:0;height:3px;width:0;background:var(--violet);z-index:120;
  transition:width .08s linear}

/* ---------- purple navigation rail (scoped so the top-bar menu stays light) ---------- */
@media screen and (min-width:1000px){
  .siderail{background:var(--violet-dim);border-right:none}
}
.siderail .rail-home{color:#fff}
.siderail .siderail .navsec{color:rgba(255,255,255,.45)}
.siderail .navgrp>details>summary{color:rgba(255,255,255,.92)}
.siderail .navgrp .cn{color:rgba(255,255,255,.45)}
.siderail .navgrp>details>summary::after{color:rgba(255,255,255,.5)}
.siderail .navgrp>details>summary:hover{background:rgba(255,255,255,.10)}
.siderail .navgrp>details[open]>summary{background:rgba(255,255,255,.10);color:#fff}
.siderail .navgrp.is-active>details>summary{background:#fff;color:var(--violet-dim);border-radius:7px}
.siderail .navgrp.is-active>details>summary .cn{color:var(--violet)}
.siderail .navgrp.is-active>details>summary::after{color:var(--violet)}
.siderail .navtools{border-left-color:rgba(255,255,255,.22)}
.siderail .navtools a{color:rgba(255,255,255,.78)}
.siderail .navtools a:hover{background:rgba(255,255,255,.12);color:#fff}
.siderail .navall{color:rgba(255,255,255,.95) !important;font-weight:400}
.siderail .navtools a.is-here{background:#fff;color:var(--violet-dim);box-shadow:inset 2px 0 0 #fff}
.siderail .navflat{color:rgba(255,255,255,.92)}
.siderail .navflat:hover{background:rgba(255,255,255,.12);color:#fff}
.siderail ::-webkit-scrollbar{width:9px}
.siderail ::-webkit-scrollbar-thumb{background:rgba(255,255,255,.25);border-radius:9px}
.siderail .navgrp>details>summary:focus-visible{outline:2px solid #fff;outline-offset:2px}

.lineup a.tlink{color:var(--ink-2);text-decoration:none}
.lineup a.tlink:hover{color:var(--violet)}

.phead + .psec > .pseclabel{margin-top:0}

@media print{.lineup a.tlink{color:var(--ink);border:none}}
@media print{.progress{display:none !important}}
@media print{a.site{color:var(--ink);border:none}a.site::after{content:""}}
@media print{.topbar,.jump,.siderail{display:none !important}body{padding-left:0}}
@media print{
    body{font-size:10.5pt;background:#fff}
  .wrap{max-width:none;padding:0}
  @page{size:letter;margin:0.55in 0.6in}
  @page glancepage{size:letter landscape;margin:0.28in 0.45in}
  .mast{padding:0.42in 0 0.35in;border:none;page-break-after:always}
  .finder,  .howto{page-break-after:always;border:none;padding-bottom:30px}
  .contents{page-break-after:always;border:none}
  .chap-open{page-break-after:always;border:none;padding-top:1.4in}
  .glance{page-break-after:always;border:none}
  .glance{page:glancepage}
  table.gtable{border:none;border-radius:0;border-collapse:collapse}
  .gtable th{background:#fff;padding:0 9pt 4pt 0;font-size:7pt}
  .gtable tr{break-inside:avoid}
  .glead{font-size:8.7pt;line-height:1.22}
  .gdetail{font-size:7.9pt;line-height:1.26;margin-top:1.2pt}
  .gtable td{padding:2.2pt 8pt 2.2pt 0;border-bottom:1px solid var(--border)}
  .gtable td.tname{font-size:9.5pt}
  .gtable col:nth-child(1){width:9% !important}
  .gtable col:nth-child(2){width:29% !important}
  .gtable col:nth-child(3){width:28% !important}
  .gtable col:nth-child(4){width:34% !important}
  .glance{padding:0}
  .glance h3{font-size:15pt}
  .glance .gcap{margin-bottom:9pt;font-size:8.5pt}
  .profile{page-break-before:always;border:none;padding:0}
  .band{break-inside:auto}
  .factor{break-inside:avoid}
  .bandlabel{break-after:avoid}
  .band:last-child{margin-bottom:0}
  .factor:last-child{margin-bottom:0}
  .ledger{break-inside:avoid;print-color-adjust:exact;-webkit-print-color-adjust:exact}
  .pcell{print-color-adjust:exact;-webkit-print-color-adjust:exact}
  .psec{break-inside:avoid}
  .bands{display:block;margin-top:14pt}
  .bands .tcol{display:block}
  .bands::after{content:"";display:block;clear:both}
  .bands .tcol{float:left;width:48.5%}
  .bands .tcol:first-child{margin-right:3%}
  .band{margin-bottom:11pt}
  .factor{margin-bottom:12pt}
  .fb>li{font-size:10.5pt;margin:0 0 4pt;padding-left:11pt}
  .fb>li::before{width:3.5pt;height:3.5pt;left:1pt}
  .fb>li>ul>li{font-size:9.5pt;margin:0 0 2.5pt;padding-left:9pt}
  .fb>li>ul>li::before{width:4pt;height:1.2pt}
  .factor p{font-size:10.5pt;line-height:1.42}
  .phead h4{font-size:17pt}
    .psec{margin-top:12pt}
  .ledger{margin-top:12pt;padding:10pt 12pt}
  .ledger .ideal{font-size:10.5pt}
  .ledger .pair{gap:8pt}
  .pcell{padding:8pt 10pt}
  .ledger p{font-size:10.5pt;line-height:1.42}
  .bandlabel,.pseclabel{padding-top:0;margin-bottom:5pt}
  .gtable td.tname a{text-decoration:none;border-bottom:none;color:#000}
  a{color:var(--violet-dim);text-decoration:none}
  .mast,.howto,.contents,.chap-open{position:relative;min-height:9.45in}
  .glance{position:relative;min-height:7.5in}
  .ppage{position:relative;min-height:9.5in}
  .ppage.p2{page-break-before:always}
  .pfoot{font-weight:400;display:block;position:absolute;bottom:0;left:0;right:0;text-align:center;font-family:var(--sans);font-size:7pt;letter-spacing:.12em;text-transform:uppercase;color:var(--slate)}
  .runhead{display:block;font-family:var(--sans);font-weight:600;font-size:8.5pt;letter-spacing:.14em;text-transform:uppercase;color:var(--slate);border-bottom:1px solid var(--border);padding-bottom:6pt;margin-bottom:12pt}
  .runhead b{color:var(--ink)}
}
.howto h3{font-family:var(--sans);font-weight:700;font-size:17px;letter-spacing:-.01em;margin:11px 0 6px}
.diybox{border-left:2px solid var(--violet);padding-left:16px;margin:10px 0}
.diybox p{margin-bottom:5px}
.diybox p:last-child{margin-bottom:0}
.contents.mastcontents{min-height:0;margin-top:20px}
.contents.mastcontents h2{font-size:22px;margin-bottom:10px}
